import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  cardHeader: {
    flexDirection: 'row',
    alignItems: 'center',
    marginHorizontal: 15,
    marginBottom: 10,
  },

  cardHeaderText: {
    marginHorizontal: 60,
    color: '#979797',
    fontSize: 18,
    fontWeight: 'bold',
  },
});